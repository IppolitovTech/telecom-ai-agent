services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - hf_cache:/app/.cache/huggingface
      - ./knowledge:/app/knowledge

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"

volumes:
  hf_cache:
